# خطة التطوير والتكامل الشاملة لمنصة مُبدع AI
## دمج Video Mixer + Workflow System + التطبيق الحالي

---

## 🎯 الهدف الاستراتيجي

تطوير منصة مُبدع AI الموحدة التي تجمع بين:
- **التطبيق الحالي** (إنشاء المحتوى الأساسي)
- **Video Mixer** (تحرير وإنتاج الفيديوهات المتقدم)
- **Workflow System** (أتمتة سير العمل المُبدعي)
- **خدمات Nginx** (البنية التحتية المحسنة)

---

## 📋 المرحلة الأولى: التحليل والتخطيط (أسبوع 1)

### 🔍 1.1 تحليل البنية الحالية

#### التطبيق الحالي - نقاط القوة:
✅ **واجهة مستخدم متكاملة** مع 7 خدمات رئيسية  
✅ **Backend FastAPI** مع Google GenAI  
✅ **خدمات AI متقدمة** (Imagen 4.0, Veo 2.0, Gemini 2.0)  
✅ **تصميم عربي RTL** مع Tailwind CSS  
✅ **نظام إدارة الاستخدام** والمراقبة  

#### Video Mixer - الميزات الفريدة:
🎬 **تقنية Veo-3** الأحدث لتوليد الفيديو  
🎬 **واجهة تحرير متقدمة** مع معاينة فورية  
🎬 **نظام إدارة المشاريع** للفيديوهات  
🎬 **تحسين الذاكرة** ومعالجة الملفات الكبيرة  

#### Workflow System - القدرات المطلوبة:
⚙️ **أتمتة سير العمل** للمحتوى المعقد  
⚙️ **نظام القوالب** للمشاريع المتكررة  
⚙️ **إدارة التبعيات** بين الخطوات  
⚙️ **مراقبة التقدم** في الوقت الفعلي  

### 🏗️ 1.2 تصميم البنية الموحدة

```
مُبدع AI - المنصة الموحدة
├── Frontend (React + TypeScript)
│   ├── الواجهة الحالية (Dashboard, Generators)
│   ├── Video Mixer Studio (تحرير الفيديو المتقدم)
│   ├── Workflow Builder (بناء سير العمل)
│   └── Project Manager (إدارة المشاريع)
├── Backend (FastAPI + Python)
│   ├── الخدمات الحالية (Image, Video, Audio, Text)
│   ├── Video Processing Service (معالجة متقدمة)
│   ├── Workflow Engine (محرك سير العمل)
│   └── Project Management API
├── Infrastructure (Nginx + Docker)
│   ├── Load Balancing (توزيع الأحمال)
│   ├── File Storage (تخزين الملفات)
│   ├── Caching Layer (طبقة التخزين المؤقت)
│   └── Security & Monitoring (الأمان والمراقبة)
└── Database Layer
    ├── User Projects (مشاريع المستخدمين)
    ├── Workflow Templates (قوالب سير العمل)
    ├── Asset Library (مكتبة الأصول)
    └── Usage Analytics (تحليلات الاستخدام)
```

---

## 🚀 المرحلة الثانية: التطوير التدريجي (أسابيع 2-8)

### 📅 الأسبوع الثاني: دمج Video Mixer

#### 2.1 تكامل Video Mixer مع التطبيق الحالي

**المهام التقنية:**

```typescript
// إضافة مسارات جديدة في App.tsx
import VideoMixerStudio from './pages/VideoMixerStudio'
import ProjectManager from './pages/ProjectManager'

// المسارات الجديدة
<Route path="/video-studio" element={<VideoMixerStudio />} />
<Route path="/projects" element={<ProjectManager />} />
```

**تحديث Sidebar.tsx:**
```typescript
const newMenuItems = [
  // العناصر الحالية...
  {
    path: '/video-studio',
    name: 'استوديو الفيديو',
    icon: <VideoStudioIcon />
  },
  {
    path: '/projects',
    name: 'إدارة المشاريع',
    icon: <ProjectIcon />
  }
]
```

**دمج خدمات Video Mixer:**
```python
# backend/services/video_mixer_service.py
class VideoMixerService:
    def __init__(self):
        self.veo3_client = genai.Client(api_key=os.environ.get("GEMINI_API_KEY"))
        self.model = "veo-3.0-generate-preview"
    
    async def create_video_project(self, project_data):
        """إنشاء مشروع فيديو جديد"""
        pass
    
    async def edit_video_description(self, video_id, new_description):
        """تحرير وصف الفيديو وإعادة الإنتاج"""
        pass
    
    async def get_video_variations(self, base_video_id):
        """إنشاء تنويعات من فيديو أساسي"""
        pass
```

#### 2.2 تطوير واجهة Video Studio

**إنشاء مكونات جديدة:**

```typescript
// frontend/src/pages/VideoMixerStudio.tsx
const VideoMixerStudio: React.FC = () => {
  const [projects, setProjects] = useState<VideoProject[]>([])
  const [activeProject, setActiveProject] = useState<VideoProject | null>(null)
  
  return (
    <div className="video-studio-container">
      <VideoProjectList projects={projects} onSelect={setActiveProject} />
      {activeProject && (
        <VideoEditor project={activeProject} onSave={handleSaveProject} />
      )}
      <VideoPreview project={activeProject} />
    </div>
  )
}
```

**مكونات فرعية:**
- `VideoProjectList.tsx` - قائمة المشاريع
- `VideoEditor.tsx` - محرر الفيديو
- `VideoPreview.tsx` - معاينة الفيديو
- `VideoExportDialog.tsx` - تصدير الفيديو

### 📅 الأسبوع الثالث: تطوير Workflow Engine

#### 3.1 بناء محرك سير العمل

**تعريف أنواع البيانات:**
```typescript
// shared/types/workflow.ts
export enum WorkflowStepType {
  TEXT_GENERATION = 'text_generation',
  IMAGE_GENERATION = 'image_generation',
  VIDEO_GENERATION = 'video_generation',
  AUDIO_GENERATION = 'audio_generation',
  STORYBOARD_GENERATION = 'storyboard_generation',
  VIDEO_EDITING = 'video_editing',
  CONTENT_ANALYSIS = 'content_analysis',
  CONDITIONAL_LOGIC = 'conditional_logic',
  DELAY = 'delay',
  MERGE = 'merge'
}

export interface WorkflowStep {
  id: string
  name: string
  type: WorkflowStepType
  config: Record<string, any>
  depends_on: string[]
  timeout_seconds: number
  retry_config: RetryConfig
  position: { x: number; y: number }
}

export interface WorkflowDefinition {
  id: string
  name: string
  description: string
  steps: WorkflowStep[]
  variables: Record<string, any>
  settings: WorkflowSettings
  tags: string[]
  created_at: string
  updated_at: string
}
```

**محرك التنفيذ:**
```python
# backend/services/workflow_engine.py
class WorkflowEngine:
    def __init__(self):
        self.step_executors = {
            'text_generation': TextGenerationExecutor(),
            'image_generation': ImageGenerationExecutor(),
            'video_generation': VideoGenerationExecutor(),
            'video_editing': VideoEditingExecutor(),
            # ... المزيد
        }
    
    async def execute_workflow(self, workflow: WorkflowDefinition):
        """تنفيذ سير العمل بالكامل"""
        execution = WorkflowExecution(workflow_id=workflow.id)
        
        try:
            # ترتيب الخطوات حسب التبعيات
            sorted_steps = self.topological_sort(workflow.steps)
            
            for step in sorted_steps:
                await self.execute_step(step, execution)
                
            execution.status = WorkflowStatus.COMPLETED
            
        except Exception as e:
            execution.status = WorkflowStatus.FAILED
            execution.error_message = str(e)
            
        return execution
    
    async def execute_step(self, step: WorkflowStep, execution: WorkflowExecution):
        """تنفيذ خطوة واحدة"""
        executor = self.step_executors.get(step.type)
        if not executor:
            raise ValueError(f"No executor found for step type: {step.type}")
        
        result = await executor.execute(step.config, execution.context)
        execution.add_step_result(step.id, result)
        
        return result
```

#### 3.2 تطوير Workflow Builder UI

**المكون الرئيسي:**
```typescript
// frontend/src/pages/WorkflowBuilder.tsx
const WorkflowBuilder: React.FC = () => {
  const [workflow, setWorkflow] = useState<WorkflowDefinition>(createEmptyWorkflow())
  const [selectedStep, setSelectedStep] = useState<WorkflowStep | null>(null)
  const [isExecuting, setIsExecuting] = useState(false)
  
  return (
    <div className="workflow-builder">
      <WorkflowToolbar 
        onAddStep={handleAddStep}
        onSave={handleSaveWorkflow}
        onExecute={handleExecuteWorkflow}
      />
      
      <div className="workflow-canvas">
        <WorkflowCanvas 
          workflow={workflow}
          selectedStep={selectedStep}
          onSelectStep={setSelectedStep}
          onUpdateStep={handleUpdateStep}
        />
      </div>
      
      <WorkflowSidebar>
        {selectedStep && (
          <StepConfigPanel 
            step={selectedStep}
            onUpdate={handleUpdateStep}
          />
        )}
        <TemplateLibrary onApplyTemplate={handleApplyTemplate} />
      </WorkflowSidebar>
      
      {isExecuting && (
        <WorkflowExecutionMonitor 
          workflowId={workflow.id}
          onComplete={handleExecutionComplete}
        />
      )}
    </div>
  )
}
```

### 📅 الأسبوع الرابع: نظام إدارة المشاريع

#### 4.1 قاعدة البيانات والنماذج

**نماذج قاعدة البيانات:**
```python
# backend/models/project.py
from sqlalchemy import Column, String, DateTime, JSON, Text, Integer
from sqlalchemy.ext.declarative import declarative_base

Base = declarative_base()

class Project(Base):
    __tablename__ = "projects"
    
    id = Column(String, primary_key=True)
    name = Column(String, nullable=False)
    description = Column(Text)
    type = Column(String)  # 'video', 'workflow', 'mixed'
    status = Column(String, default='draft')
    config = Column(JSON)
    assets = Column(JSON)  # قائمة الملفات والأصول
    created_at = Column(DateTime)
    updated_at = Column(DateTime)
    user_id = Column(String)

class WorkflowTemplate(Base):
    __tablename__ = "workflow_templates"
    
    id = Column(String, primary_key=True)
    name = Column(String, nullable=False)
    description = Column(Text)
    category = Column(String)
    workflow_definition = Column(JSON)
    is_public = Column(Boolean, default=False)
    usage_count = Column(Integer, default=0)
    created_by = Column(String)
    created_at = Column(DateTime)
```

#### 4.2 خدمة إدارة المشاريع

```python
# backend/services/project_service.py
class ProjectService:
    def __init__(self):
        self.db = get_database_session()
    
    async def create_project(self, project_data: dict) -> Project:
        """إنشاء مشروع جديد"""
        project = Project(
            id=generate_uuid(),
            name=project_data['name'],
            description=project_data.get('description', ''),
            type=project_data['type'],
            config=project_data.get('config', {}),
            created_at=datetime.utcnow()
        )
        
        self.db.add(project)
        await self.db.commit()
        
        return project
    
    async def get_user_projects(self, user_id: str) -> List[Project]:
        """الحصول على مشاريع المستخدم"""
        return await self.db.query(Project).filter(
            Project.user_id == user_id
        ).all()
    
    async def duplicate_project(self, project_id: str) -> Project:
        """نسخ مشروع موجود"""
        original = await self.get_project(project_id)
        
        new_project = Project(
            id=generate_uuid(),
            name=f"{original.name} - نسخة",
            description=original.description,
            type=original.type,
            config=original.config.copy(),
            created_at=datetime.utcnow()
        )
        
        self.db.add(new_project)
        await self.db.commit()
        
        return new_project
```

### 📅 الأسبوع الخامس: تطوير نظام القوالب

#### 5.1 مكتبة القوالب الجاهزة

**قوالب سير العمل المحددة مسبقاً:**

```python
# backend/services/template_service.py
class TemplateService:
    def __init__(self):
        self.predefined_templates = {
            'social_media_campaign': {
                'name': 'حملة وسائل التواصل الاجتماعي',
                'description': 'إنشاء محتوى متكامل لحملة تسويقية',
                'steps': [
                    {
                        'type': 'text_generation',
                        'name': 'إنشاء النص الإعلاني',
                        'config': {'tone': 'marketing', 'length': 'medium'}
                    },
                    {
                        'type': 'image_generation',
                        'name': 'إنشاء الصورة الترويجية',
                        'config': {'style': 'commercial', 'aspect_ratio': '1:1'}
                    },
                    {
                        'type': 'video_generation',
                        'name': 'إنشاء فيديو قصير',
                        'config': {'duration': 15, 'style': 'commercial'}
                    }
                ]
            },
            
            'product_showcase': {
                'name': 'عرض المنتج',
                'description': 'محتوى شامل لعرض منتج جديد',
                'steps': [
                    {
                        'type': 'content_analysis',
                        'name': 'تحليل معلومات المنتج',
                        'config': {'analysis_type': 'product_features'}
                    },
                    {
                        'type': 'storyboard_generation',
                        'name': 'إنشاء ستوري بورد',
                        'config': {'scenes': 5, 'style': 'commercial'}
                    },
                    {
                        'type': 'video_generation',
                        'name': 'إنتاج فيديو العرض',
                        'config': {'duration': 30, 'style': 'product_demo'}
                    },
                    {
                        'type': 'text_generation',
                        'name': 'كتابة وصف المنتج',
                        'config': {'tone': 'professional', 'include_features': True}
                    }
                ]
            },
            
            'educational_content': {
                'name': 'محتوى تعليمي',
                'description': 'إنشاء مواد تعليمية تفاعلية',
                'steps': [
                    {
                        'type': 'text_generation',
                        'name': 'كتابة المحتوى التعليمي',
                        'config': {'tone': 'educational', 'structure': 'lesson_plan'}
                    },
                    {
                        'type': 'image_generation',
                        'name': 'إنشاء الرسوم التوضيحية',
                        'config': {'style': 'educational', 'count': 3}
                    },
                    {
                        'type': 'video_generation',
                        'name': 'إنتاج فيديو تعليمي',
                        'config': {'duration': 60, 'style': 'educational'}
                    },
                    {
                        'type': 'audio_generation',
                        'name': 'إنشاء التعليق الصوتي',
                        'config': {'voice': 'professional', 'language': 'ar'}
                    }
                ]
            }
        }
```

#### 5.2 واجهة مكتبة القوالب

```typescript
// frontend/src/components/TemplateLibrary.tsx
const TemplateLibrary: React.FC = () => {
  const [templates, setTemplates] = useState<WorkflowTemplate[]>([])
  const [selectedCategory, setSelectedCategory] = useState<string>('all')
  const [searchTerm, setSearchTerm] = useState<string>('')
  
  const categories = [
    { id: 'all', name: 'جميع القوالب', icon: '📋' },
    { id: 'social_media', name: 'وسائل التواصل', icon: '📱' },
    { id: 'marketing', name: 'التسويق', icon: '📈' },
    { id: 'education', name: 'التعليم', icon: '🎓' },
    { id: 'entertainment', name: 'الترفيه', icon: '🎬' },
    { id: 'business', name: 'الأعمال', icon: '💼' }
  ]
  
  return (
    <div className="template-library">
      <div className="template-search">
        <input
          type="text"
          placeholder="البحث في القوالب..."
          value={searchTerm}
          onChange={(e) => setSearchTerm(e.target.value)}
          className="search-input"
        />
      </div>
      
      <div className="template-categories">
        {categories.map(category => (
          <button
            key={category.id}
            onClick={() => setSelectedCategory(category.id)}
            className={`category-btn ${selectedCategory === category.id ? 'active' : ''}`}
          >
            <span className="category-icon">{category.icon}</span>
            <span className="category-name">{category.name}</span>
          </button>
        ))}
      </div>
      
      <div className="template-grid">
        {filteredTemplates.map(template => (
          <TemplateCard
            key={template.id}
            template={template}
            onSelect={handleSelectTemplate}
            onPreview={handlePreviewTemplate}
          />
        ))}
      </div>
    </div>
  )
}
```

### 📅 الأسبوع السادس: تحسين البنية التحتية

#### 6.1 تحسين إعدادات Nginx

**تحديث nginx.conf للمنصة الموحدة:**
```nginx
# nginx/nginx.conf - تحسينات للمنصة الموحدة
http {
    # تحسينات الأداء للملفات الكبيرة
    client_max_body_size 500M;
    client_body_timeout 300s;
    client_header_timeout 300s;
    
    # تحسين التخزين المؤقت
    proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=video_cache:100m 
                     max_size=10g inactive=60m use_temp_path=off;
    
    # ضغط محسن للملفات المختلفة
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml
        video/mp4
        audio/mpeg;
    
    # تحديد معدل الطلبات المحسن
    limit_req_zone $binary_remote_addr zone=api:10m rate=20r/s;
    limit_req_zone $binary_remote_addr zone=upload:10m rate=5r/s;
    limit_req_zone $binary_remote_addr zone=generation:10m rate=2r/s;
}
```

**إعدادات خاصة بالخدمات الجديدة:**
```nginx
# nginx/conf.d/mubdi-ai-enhanced.conf
server {
    listen 443 ssl http2;
    server_name api.mubdi-ai.example.com;
    
    # Video processing endpoints
    location /api/v1/video-mixer/ {
        limit_req zone=generation burst=5 nodelay;
        proxy_pass http://backend;
        proxy_read_timeout 600s;
        proxy_send_timeout 600s;
    }
    
    # Workflow execution endpoints
    location /api/v1/workflow/ {
        limit_req zone=api burst=10 nodelay;
        proxy_pass http://backend;
        proxy_read_timeout 300s;
    }
    
    # File upload endpoints
    location /api/v1/upload/ {
        limit_req zone=upload burst=3 nodelay;
        proxy_pass http://backend;
        client_max_body_size 500M;
    }
    
    # Static assets with caching
    location /assets/ {
        proxy_pass http://backend;
        proxy_cache video_cache;
        proxy_cache_valid 200 1h;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

#### 6.2 نظام تخزين الملفات المحسن

```python
# backend/services/file_storage_service.py
import os
import aiofiles
from pathlib import Path
from typing import Optional, Dict, Any

class FileStorageService:
    def __init__(self):
        self.base_path = Path(os.getenv("STORAGE_PATH", "./storage"))
        self.max_file_size = int(os.getenv("MAX_FILE_SIZE", "500")) * 1024 * 1024  # 500MB
        
        # إنشاء المجلدات المطلوبة
        self.create_directories()
    
    def create_directories(self):
        """إنشاء هيكل المجلدات"""
        directories = [
            "projects",
            "assets/images",
            "assets/videos", 
            "assets/audio",
            "assets/documents",
            "temp",
            "exports"
        ]
        
        for directory in directories:
            (self.base_path / directory).mkdir(parents=True, exist_ok=True)
    
    async def save_project_asset(
        self, 
        project_id: str, 
        file_data: bytes, 
        filename: str,
        asset_type: str
    ) -> Dict[str, Any]:
        """حفظ ملف مشروع"""
        
        # التحقق من حجم الملف
        if len(file_data) > self.max_file_size:
            raise ValueError("حجم الملف كبير جداً")
        
        # إنشاء مسار الملف
        project_path = self.base_path / "projects" / project_id
        project_path.mkdir(exist_ok=True)
        
        asset_path = project_path / asset_type
        asset_path.mkdir(exist_ok=True)
        
        file_path = asset_path / filename
        
        # حفظ الملف
        async with aiofiles.open(file_path, 'wb') as f:
            await f.write(file_data)
        
        return {
            "path": str(file_path),
            "url": f"/assets/{project_id}/{asset_type}/{filename}",
            "size": len(file_data),
            "type": asset_type
        }
    
    async def get_project_assets(self, project_id: str) -> List[Dict[str, Any]]:
        """الحصول على جميع أصول المشروع"""
        project_path = self.base_path / "projects" / project_id
        
        if not project_path.exists():
            return []
        
        assets = []
        for asset_type_path in project_path.iterdir():
            if asset_type_path.is_dir():
                for file_path in asset_type_path.iterdir():
                    if file_path.is_file():
                        stat = file_path.stat()
                        assets.append({
                            "name": file_path.name,
                            "type": asset_type_path.name,
                            "size": stat.st_size,
                            "url": f"/assets/{project_id}/{asset_type_path.name}/{file_path.name}",
                            "created_at": stat.st_ctime
                        })
        
        return assets
```

### 📅 الأسبوع السابع: التكامل والاختبار

#### 7.1 تكامل جميع الخدمات

**تحديث الواجهة الرئيسية:**
```typescript
// frontend/src/App.tsx - النسخة المحدثة
import React from 'react'
import { Routes, Route } from 'react-router-dom'
import Layout from './components/Layout'

// الصفحات الحالية
import Dashboard from './pages/Dashboard'
import ImageGenerator from './pages/ImageGenerator'
import AdvancedImageGenerator from './pages/AdvancedImageGenerator'
import VideoGenerator from './pages/VideoGenerator'
import AudioGenerator from './pages/AudioGenerator'
import AdCopyGenerator from './pages/AdCopyGenerator'
import StoryboardGenerator from './pages/StoryboardGenerator'
import Settings from './pages/Settings'

// الصفحات الجديدة
import VideoMixerStudio from './pages/VideoMixerStudio'
import WorkflowBuilder from './pages/WorkflowBuilder'
import WorkflowExecution from './pages/WorkflowExecution'
import ProjectManager from './pages/ProjectManager'
import TemplateLibrary from './pages/TemplateLibrary'

function App() {
  return (
    <div className="page-container">
      <Layout>
        <Routes>
          {/* المسارات الحالية */}
          <Route path="/" element={<Dashboard />} />
          <Route path="/images" element={<AdvancedImageGenerator />} />
          <Route path="/images/basic" element={<ImageGenerator />} />
          <Route path="/videos" element={<VideoGenerator />} />
          <Route path="/audio" element={<AudioGenerator />} />
          <Route path="/ad-copy" element={<AdCopyGenerator />} />
          <Route path="/storyboard" element={<StoryboardGenerator />} />
          <Route path="/settings" element={<Settings />} />
          
          {/* المسارات الجديدة */}
          <Route path="/video-studio" element={<VideoMixerStudio />} />
          <Route path="/workflow/builder" element={<WorkflowBuilder />} />
          <Route path="/workflow/execution/:id" element={<WorkflowExecution />} />
          <Route path="/projects" element={<ProjectManager />} />
          <Route path="/templates" element={<TemplateLibrary />} />
        </Routes>
      </Layout>
    </div>
  )
}

export default App
```

**تحديث القائمة الجانبية:**
```typescript
// frontend/src/components/Sidebar.tsx - إضافة العناصر الجديدة
const menuItems = [
  // العناصر الحالية...
  {
    path: '/',
    name: 'لوحة التحكم',
    icon: <DashboardIcon />
  },
  {
    path: '/images',
    name: 'إنشاء الصور',
    icon: <ImageIcon />
  },
  {
    path: '/videos',
    name: 'إنشاء الفيديو',
    icon: <VideoIcon />
  },
  
  // العناصر الجديدة
  {
    path: '/video-studio',
    name: 'استوديو الفيديو المتقدم',
    icon: <VideoStudioIcon />,
    badge: 'جديد'
  },
  {
    path: '/workflow/builder',
    name: 'بناء سير العمل',
    icon: <WorkflowIcon />,
    badge: 'جديد'
  },
  {
    path: '/projects',
    name: 'إدارة المشاريع',
    icon: <ProjectIcon />,
    badge: 'ج
